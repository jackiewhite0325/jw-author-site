# jw-author-site
Author website for J. White / Sigil &amp; Scribe, LLC

## Generating standalone book pages

Run the page generator from the repository root:

```bash
python3 gen_pages.py
```

Generated files are written to `pages/` by default. Override the destination with
the `JW_AUTHOR_SITE_OUTPUT_DIR` environment variable when needed.
